# 凡例 v2.2

> 最后修订：2026-08-28 | 监督：佩丽卡 | 起草：伊冯 | Agent 主线与证据分层补订：GPT-5.6 Sol

## 〇、总则

### 0.1 文体

**全书一律用白话文。** 编年、纪传、志、表、史论，全部用现代汉语写作。

不用文言，不用“论曰”“太史公曰”等仿古标题。叙事干净、准确、可读；议论有据、有洞察、不空谈。

### 0.2 真实性原则

每一条事实必须**可追溯**。具体要求：

- **链接可访问**：引用的 URL 在编纂时必须是活链。死链须标注“（链接已失效）”并附存档路径。
- **网页快照留档**：每一条引用的网页，原则上在 `sources/` 目录下保存快照（详见 §五）；若当前工具环境无法执行快照流程，必须明确标记 snapshot pending，不得伪造归档文件。
- **交叉验证**：关键数据（发布日期、参数规模、benchmark 分数、融资额）优先使用两个独立来源。只有唯一一手公告或暂时只有单一权威来源时，应明确标注证据等级，不把单源报道扩写成已充分证实的事实。
- **有据则书，无据则阙**。不知道也是数据；不确定的事不靠估算填空。

### 0.3 Agent 产品能力的证据分层：宣传不等于可靠性

Agent 与普通模型发布不同。模型厂商只要公开一个 API，就可以证明“这个 API 存在”；但 Agent 声称“能替用户完成任务”时，**功能入口存在、一次 demo 成功、benchmark 得分、真实用户长期可靠使用**是四种完全不同的证据。

以后涉及 Agent、Computer / Phone Use、自动化、Multi-Agent 与“AI 员工”产品时，统一区分以下四层：

| 证据层 | 可以证明什么 | 不能证明什么 |
|---|---|---|
| **厂商声明 / 产品页 / 发布会** | 厂商声称支持某能力；产品设计目标是什么 | 不能证明成功率、重复可靠性、真实成本和 ROI |
| **公开可用产品 / API** | 普通或受邀用户确实可以调用该功能；可确认 GA / beta / invite / preview 状态 | 不能证明复杂任务能长期稳定完成 |
| **独立实测 / 用户实测 / 重复执行** | 可以观察真实任务中的成功、失败、耗时、token / runtime 成本、人工接管和环境限制 | 结果仍可能受版本、账号、地区、任务和测试方法影响 |
| **生产指标 / 长期使用数据** | 可以讨论可靠性、留存、成本、组织价值、事故率和运营边界 | 仍不能自动推出“可替代某岗位”或“普遍提高生产率” |

因此写作时遵守以下规则：

- 只有厂商资料时，优先写 **“厂商称支持……” “官方产品页列出……”**，不要直接改写成“该 Agent 能稳定完成……”。
- “一次成功”只能证明 **can**，不能证明 **reliably can**。对长程 Agent，重复执行的稳定性比单次最佳轨迹更重要。
- benchmark 必须注明是否允许 retry、并行、人工接管、不同 harness、不同工具权限；`pass@k` 与连续多次都成功的可靠性不是一回事。
- 必须标注产品状态：**demo / research preview / invite-only / beta / public preview / GA / commercial**。发布会演示不得自动写成已普遍可用。
- 对 GUI / Computer / Phone Agent，记录登录、验证码、扫码、支付、高危权限、UI 变化、网络抖动等现实边界；这些往往决定真实可用性。
- 对 Multi-Agent，除并发数量外还要问：总 token / runtime、串行瓶颈、资源冲突、结果合并与人工验收成本。
- 对“本地更安全”“企业级安全”等宣传，只能先记录为设计目标；还应检查 sandbox、credential isolation、network policy、audit、Skill / plugin supply chain 与独立安全研究。
- 对收入、客户数、seat 数，要区分“购买 AI 产品”和“真实使用 autonomous Agent”；商业成功不等于技术自治程度已经成熟。

Agent 产品条目最好同时回答五个问题：

1. **Claimed capability**：厂商说它能做什么？
2. **Availability**：用户实际上能拿到什么版本？
3. **Observed behavior**：独立 / 用户实测看到什么？
4. **Known limitations**：在哪里失败、需要人工接管、成本如何？
5. **Commercial status**：怎样收费、是否进入生产、有没有长期数据？

如缺某一层，就明确写“暂无独立实测”“生产数据未公开”，不要用宣传材料替读者补完证据链。

### 0.4 模型优先，Agent 为行动权第二主线

大模型纪事的第一核心对象仍是**模型**，但 2022 年以后必须同时追踪 **Agent / 行动权演进**。本书不只记录“模型怎样变聪明”，也记录“模型怎样从输出文本走向调用工具、操作环境、接受任务、协调多个 Agent 与接触物理世界”。

整理优先级：

1. **模型发布**（新模型、重大版本更新、架构创新）——最高优先级
2. **Agent / 执行系统里程碑**（工具调用、Computer Use、coding/research agent、MCP/A2A 等协议、sandbox、memory/checkpoint、multi-agent、Agent benchmark、权限/支付/物理设备接口）——高优先级
3. **技术突破**（训练方法、推理优化、长上下文、开源工具）——高优先级
4. **公司与治理事件**（融资、并购、人事变动、政策封禁、重要标准治理）——中优先级
5. **社区/文化事件**（病毒传播、亚文化现象、争议）——选入，作为生态注脚

“模型优先”不是“只有发布新模型才值得进编年”。以下 Agent 事件即使没有伴随新模型，也应独立收录：

- 改变**行动接口**：如 Function Calling、Tool Use、Computer Use；
- 改变**工具/Agent 互操作**：如 MCP、A2A；
- 改变**运行时结构**：如 sandbox、background task、durable state、checkpoint / resume；
- 改变**组织方式**：如 supervisor/subagent、Agent Swarm、事件触发 automations；
- 改变**评价方法**：如 WebArena、OSWorld、SWE-bench、Terminal-Bench 等执行式环境评测；
- 改变**权限与现实后果**：如支付、credential delegation、物理设备控制和相应安全边界。

收录标准不是“名字里有没有 Agent”，而是：**它是否扩大、限制、标准化或重新定义了模型的行动半径。**

同一时期内，模型与 Agent 事件可以并列成两条主线。这是一部**大模型及其执行系统的历史**，不是单纯商业史、产品新闻汇编或 benchmark 排行史。

### 0.5 断限

上起 **2017 年 6 月**（Attention Is All You Need 发表），下迄**编纂之时**。

断限之前的重要前史（深度学习早期发展、word2vec、seq2seq、经典智能体 / BDI / 强化学习 Agent 等），入《志·前史》或相关专题；LLM Agent 的直接前史从 MRKL、SayCan、ReAct 等节点开始进入正文。

---

## 一、编年

### 1.1 编年是本书的主线

以年-月为基本单位，逐条记录大模型及 Agent 执行系统的重要事件。读完编年部分，应能掌握领域从**模型能力**到**行动能力**的主要演化。

编年是骨架，纪传是肌肉——编年告诉你“什么时候发生了什么”，纪传告诉你“为什么重要、怎么演变的”；志与表负责把跨年份的 Agent、算力、开源、数据等横向主线重新串起来。

### 1.2 目录组织

```text
编年/
├── 2022/
│   ├── 08.md          # 2022 年 8 月
│   └── 12.md          # 2022 年 12 月
├── 2023/
│   ├── 02.md
│   ├── 03.md
│   ├── 05.md
│   └── 06.md          # Function Calling / Agent 工具接口
├── ...
```

**规则**：

- 每年一个文件夹，每月一个文件。
- 文件名统一为 `MM.md`（两位数字月份）。同一月份有多个专题时，在同一文件内用 `##` 二级标题分隔，不拆多个文件。
- 该月无重要事件则不建文件。
- 年份文件夹内始终保留 `.gitkeep`，确保空年份目录不被 Git 忽略。
- Agent 事件不另建平行“Agent 编年”目录；仍进入正常年月，避免把模型史与行动史割裂成两本书。

### 1.3 事件条目格式

每条编年事件采用**单行概述格式**：

```text
**YYYY-MM-DD** — 事件概述。关键参与者。出处。[^n]
```

示例：

```text
**2017-06-12** — Attention Is All You Need 论文发布。Vaswani et al. (Google Brain / Google Research) 提出 Transformer 架构。arXiv:1706.03762
**2022-11-30** — OpenAI 发布 ChatGPT，基于 GPT-3.5。五天内用户破百万。出处：OpenAI Blog, 2022-11-30
**2023-06-13** — OpenAI 发布 Function Calling，模型可依据 schema 输出结构化函数参数，Agent 工具调用开始 API 原语化。
```

- 日期明确到日；仅有月份的写到月；仅有年份的写到年。
- **日期范围用短横 `-`**，不用波浪号 `~`。如 `2023-02-14~15`→`2023-02-14—15`。
- 出处紧跟在描述后，同一行内给出。
- 多源交叉验证后的事件，不额外标注。单源事件必须体现证据等级。
- 不确定的日期写“约YYYY年M月”。

### 1.4 专题深度条目

当某个事件有足够纵深来支撑一篇深度文章时，可以写成**专题条目**——有导读 blockquote、分节标题、分析段落。典型特征是事件条目的延展，不脱编年的时间轴。当前已有 SD、R1、Sydney、318、Function Calling 等条目属此类。

Agent 事件若跨越多个层级，应在月度条目中说明其**当时意义**，在《志·AI Agent 生态》中再写长期演化；避免用 2026 年终局知识假装当时参与者已经知道结果。

---

## 二、纪传

### 2.1 纪传分类

- **本纪**：公司兴衰（如《OpenAI本纪》）、核心人物。以编年为纲叙事。
- **世家**：模型系列源流（如《GPT世家》《Llama世家》）。叙其迭代、盛衰。
- **列传**：单个模型、架构、技术或具有独立谱系的重要产品系统（如《Transformer列传》《DeepSeek-R1列传》《Codex / Copilot列传》）。

Agent 通常优先写入“志”和相关产品/模型列传；只有形成持续、独立的谱系后才单独列传，避免为每个短命 Agent 框架立传。

### 2.2 纪年

统一用公元纪年，年-月-日格式。不再使用干支纪年。

### 2.3 称谓

- 首次出现写全称+简称，后续用简称。
- 人物首次出现注其身份。
- 模型首次出现注已公开且可靠的参数规模与发布日期；未披露则写未披露，不填行业传闻。
- Agent / protocol 首次出现说明它解决的是哪一层问题（tool、Agent-to-Agent、runtime、UI、payment、hardware 等），不要只抄营销定义。
- Agent 产品能力若只有厂商材料，应使用“官方称”“厂商列出”而非无条件陈述；如有独立实测或明确限制，应与宣传口径并列。
- 叙事与议论分离——不以今日之见褒贬当时之事。

### 2.4 史料出处

每一条关键事实须注明出处，使用脚注格式：

```text
[^1]: Attention Is All You Need, Vaswani et al., 2017. https://arxiv.org/abs/1706.03762
[^2]: OpenAI Blog, "GPT-4", 2023-03-14. https://openai.com/research/gpt-4
```

必注出处的情形：发布日期/参数规模、事件日期与参与者、benchmark 数据、协议版本、产品能力边界、引用他人言论、争议性事实。

---

## 三、史论

### 3.1 定位

**论**（史论）是独立的议论文章，放在 `论/` 目录中。用于全书之序、重要篇目之评论、卷末总论。

Agent 史论重点讨论制度与因果：行动权为何下放、为什么某些场景先跑通、验证与权限怎样限制自主性、multi-agent 为什么成为调度问题。不要把“AI 像不像人”当作主要分析框架。

### 3.2 使用「评曰」

在编年或纪传中的议论用 **「评曰」** 作为小节标题。

- 先叙后议：先概括篇中要旨，再发议论。
- 议论须有史识——不是感慨，是洞察。
- 可引用国际案例作为对照。
- 篇幅以 100-300 字为宜；专题长条目可适当扩展。
- **纯白话，不仿文言**。

---

## 四、志、表

- **志**记专题。跨越年份的主题（算力变迁、数据之争、开源运动、评价基准演化、社区文化、AI Agent 生态），不宜拆散入编年，单独成志。
- **表**备速查。大事年表、版本沿革表、公司兴替表、Agent 发展大事表，以表格形式梳理，不作长篇叙事。

二者均为白话，叙事简洁。

Agent 专题的分工：

- 《志·AI Agent 生态》：完整结构史与技术栈；
- 《志·Agent 产品与商业化》：全球商业化与计费方式；
- 《志·中国 Agent 生态与商业化》：中国模型、平台、云和产品路线；
- 《志·个人 Agent 生态与商业化》：C 端桌面、本地、记忆、云工作台和 Phone Agent；
- 《志·OpenClaw 与中国 Agent“龙虾潮”》：OpenClaw 的产品文化扩散、中国衍生生态及宣传—实测差距；
- 《论·Agent 时代》：行动权、组织与经济学史论；
- 《表·Agent 发展大事表》：逐节点速查；
- 编年：把最重要 Agent 转折放回其真实年月。

---

## 五、sources/ 归档规范

### 5.1 目录组织

```text
sources/
├── 2022/
│   ├── 08/
│   │   ├── index.json            # 该月来源索引
│   │   ├── stability-blog.html   # 网页快照
│   │   └── wiki-sd.html
│   └── 12/
│       └── ...
├── 2023/
│   └── ...
```

- 按 `年份/月份/` 组织，与编年目录一一对应。
- 专题 / 纪传 / 表可在 `sources/志/...`、`sources/论/...`、`sources/纪传/...`、`sources/表/...` 建独立索引。
- 每个来源文件命名为 `来源域名-文章标识.html`。
- 如引用同一来源的多篇文章，加数字后缀：`verge-01.html`, `verge-02.html`。

### 5.2 快照格式

- **主快照**：HTML 文件（`curl -L` 抓取，保留文本内容即可，不需要 CSS/JS/图片）。
- **辅助快照**：截图（PNG，仅用于 twitter/知乎等动态页面或需要视觉留证的场景）。
- 截图文件较大时（单张 > 1MB），上传到 GitHub Releases，`index.json` 中记录 Release URL。
- 快照文件名不含空格，用 ASCII 字母 + 数字 + 连字符。
- 工具环境无法执行抓取时，只写 `snapshot: null` / `status: ...pending`，**绝不创建假的 HTML 快照**。

### 5.3 index.json 格式

```json
{
  "month": "2022-08",
  "sources": [
    {
      "ref": "[^1]",
      "url": "https://stability.ai/news/stable-diffusion-public-release",
      "title": "Stable Diffusion Public Release",
      "snapshot": "stability-blog.html",
      "screenshot": null,
      "archived_at": "2026-05-25",
      "wayback_url": "https://web.archive.org/web/20260525120000/https://stability.ai/..."
    }
  ]
}
```

- `wayback_url`：通过 Internet Archive Save Page Now API 保存后的永久链接（推荐但不强制）。
- 单一权威来源事件可用 `status` 明确记录证据等级，例如 `single_authoritative_source_snapshot_pending`。

### 5.4 归档流程

1. 撰写条目时，用 `tools/validate_links.js` 验证所有 URL 可达性。
2. 用 `curl -L <url> > sources/YYYY/MM/slug.html` 保存文本快照。
3. 更新 `index.json`。
4. 提交 Git 时 HTML 文件一并入库。截图文件大于 1MB 的走 GitHub Releases。

---

## 六、交叉引用

### 6.1 纪传 → 编年

纪传中引用编年条目时，用以下格式：

```text
见《编年·YYYY年M月》
```

示例：*GPT-3 于 2020 年 6 月发布，因其涌现能力而迅速成为研究焦点（详见《编年·2020年6月》）。*

**规则**：
- 引用格式统一为书名号 + 年-月，不写具体文件名（`2020/06.md`→`《编年·2020年6月》`）。
- 只在纪传中引用编年已收录的事件。编年尚未收录的，纪传中直接给出日期和出处，不含交叉引用。
- 交叉引用不是用来省事的——纪传仍应给出足够上下文，让读者不跳转也能理解。

### 6.2 编年 → 纪传 / 志 / 表

编年条目末尾，如该事件已有详细纪传或专题，追加提示：

```text
> 📖 详见《GPT 世家》《志·AI Agent 生态》《表·Agent 发展大事表》。
```

**规则**：
- 仅当被引用正文已存在时才添加提示。
- 提示放在条目末尾、出处注脚之前。
- 一个编年条目可引用多篇正文。

### 6.3 志 → 编年/纪传

志中引用编年条目：`见《编年·YYYY年M月》`。
志中引用纪传：`见《[纪传名]》`。

### 6.4 交叉引用索引

根目录 `INDEX.md` 为**自动生成索引**。当条目或交叉引用变化后，应运行仓库工具重新生成；**禁止手工编辑 INDEX.md**，避免人工内容与生成器状态分叉。

---

## 七、审校规则

### 7.1 交叉验证

- 日期、参数、benchmark 数据优先使用**两个独立来源**。
- 官方发布本身只有唯一一手来源时，可以引用该一手来源；如果结论依赖外部报道、匿名消息或单一媒体，则必须标注证据等级。
- 自媒体（Twitter/知乎/公众号）可作线索，不可作争议事实的唯一出处。
- **维基百科**可作为交叉验证辅助来源，不可作争议事实的唯一出处。引用时应优先锁定版本并归档。
- Agent benchmark 必须区分**模型、harness、工具权限、环境版本和是否允许并行 / 重试**；不同 scaffolding 的分数不可直接当成模型裸能力比较。
- Agent 产品若依赖厂商 benchmark / demo，必须与“公开可用状态”和“真实使用证据”分栏理解；没有独立证据时明确写缺口。
- 用户实测、社区帖子可以记录真实失败模式，但要标明其样本局限，不能从单个体验推成总体结论。

### 7.2 笔削

- 有据则书，无据则阙。
- 争议事件须列各方说法。
- 商业宣传声称须与其他独立评测对照后再采信。
- 不把“第一个 AI 员工”“完全自主”“AGI Agent”“一人公司”等营销标签直接写成史实判断。
- 对 Agent 必须区分：**模型能力、harness 能力、工具能力、权限范围、执行环境、产品可用状态、重复可靠性与最终任务完成率**。
- 对“节省 X% 人力”“替代 X% 岗位”“全程无人值守”等说法，除非有可核验生产数据，否则必须明确归属为厂商 / 客户宣传口径。
- 对 GUI / Computer / Phone Agent，优先记录失败后的真实世界状态和恢复方式，而不只记录最终成功截图。