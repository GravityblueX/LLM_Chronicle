# 凡例 v2.0

> 最后修订：2026-05-25 | 监督：佩丽卡 | 起草：伊冯

## 〇、总则

### 0.1 文体

**全书一律用白话文。** 编年、纪传、志、表、史论，全部用现代汉语写作。

不用文言，不用"论曰""太史公曰"等仿古标题。叙事干净、准确、可读；议论有据、有洞察、不空谈。

### 0.2 真实性原则

每一条事实必须**可追溯**。具体要求：

- **链接可访问**：引用的 URL 在编纂时必须是活链。死链须标注"（链接已失效）"并附存档路径。
- **网页快照留档**：每一条引用的网页，必须在 `sources/` 目录下保存快照（详见 §五）。
- **交叉验证**：关键数据（发布日期、参数规模、benchmark 分数、融资额）至少两个独立来源。单源信息标注"（存疑）"。
- **有据则书，无据则阙**。不确定的事不写，写进去的必须能查回去。

### 0.3 模型优先

大模型纪事的核心对象是**模型**。整理优先级：

1. **模型发布**（新模型、重大版本更新、架构创新）——最高优先级
2. **技术突破**（训练方法、推理优化、开源工具）——高优先级
3. **公司事件**（融资、并购、人事变动、政策封禁）——中优先级
4. **社区/文化事件**（病毒传播、亚文化现象、争议）——选入，作为生态注脚

同一时段内，模型事件优先于公司事件优先于社区事件。这不是说后者不重要——而是说这是一部**模型的历史**，不是一部商业史或文化史。

### 0.4 断限

上起 **2017 年 6 月**（Attention Is All You Need 发表），下迄**编纂之时**。

断限之前的重要前史（深度学习早期发展、word2vec、seq2seq 等），入《志·前史》。

---

## 一、编年

### 1.1 编年是本书的主线

以年-月为基本单位，逐条记录大模型领域的重要事件。读完编年部分即可掌握领域全貌。

编年是骨架，纪传是肌肉——编年告诉你"什么时候发生了什么"，纪传告诉你"为什么重要、怎么演变的"。

### 1.2 目录组织

```
编年/
├── 2022/
│   ├── 08.md          # 2022 年 8 月
│   └── 12.md          # 2022 年 12 月
├── 2023/
│   ├── 02.md
│   ├── 03.md
│   └── 05.md
├── ...
```

**规则**：

- 每年一个文件夹，每月一个文件。
- 文件名统一为 `MM.md`（两位数字月份）。同一月份有多个专题时，在同一文件内用 `##` 二级标题分隔，不拆多个文件。
- 该月无事则不建文件。
- 年份文件夹内始终保留 `.gitkeep`，确保空年份目录不被 Git 忽略。

### 1.3 事件条目格式

每条编年事件采用**单行概述格式**：

```
**YYYY-MM-DD** — 事件概述。关键参与者。出处。[^n]
```

示例：

```
**2017-06-12** — Attention Is All You Need 论文发布。Vaswani et al. (Google Brain / Google Research) 提出 Transformer 架构。arXiv:1706.03762
**2022-11-30** — OpenAI 发布 ChatGPT，基于 GPT-3.5。五天内用户破百万。出处：OpenAI Blog, 2022-11-30
```

- 日期明确到日；仅有月份的写到月；仅有年份的写到年。
- **日期范围用短横 `-`**，不用波浪号 `~`。如 `2023-02-14~15`→ `2023-02-14—15`。
- 出处紧跟在描述后，同一行内给出。
- 多源交叉验证后的事件，不额外标注。单源事件标"（存疑）"。
- 不确定的日期写"约YYYY年M月"。

### 1.4 专题深度条目

当某个事件有足够的纵深来支撑一篇深度文章时，可以写成**专题条目**——有导读 blockquote、分节标题、分析段落。典型特征是事件条目的延展，不脱编年的时间轴。当前已有的 SD、R1、Sydney、318 条目均属此类。

---

## 二、纪传

### 2.1 纪传分类

- **本纪**：公司兴衰（如《OpenAI本纪》）、核心人物（如《林俊旸本纪》）。以编年为纲叙事。
- **世家**：模型系列源流（如《GPT世家》《Llama世家》）。叙其迭代、盛衰。
- **列传**：单个模型、架构、技术的始末（如《Transformer列传》《DeepSeek-R1列传》）。

### 2.2 纪年

统一用公元纪年，年-月-日格式。不再使用干支纪年。

### 2.3 称谓

- 首次出现写全称+简称（如"OpenAI"），后续用简称。
- 人物首次出现注其身份。
- 模型首次出现注参数规模与发布日期。
- 叙事与议论分离——不以今日之见褒贬当时之事。

### 2.4 史料出处

每一条关键事实须注明出处，使用脚注格式：

```
[^1]: Attention Is All You Need, Vaswani et al., 2017. https://arxiv.org/abs/1706.03762
[^2]: OpenAI Blog, "GPT-4", 2023-03-14. https://openai.com/research/gpt-4
```

必注出处的情形：发布日期/参数规模、事件日期与参与者、benchmark 数据、引用他人言论、争议性事实。

---

## 三、史论

### 3.1 定位

**论**（史论）是独立的议论文章，放在 `论/` 目录中。用于全书之序、重要篇目之评论、卷末总论。

### 3.2 使用「评曰」

在编年或纪传中的议论用 **「评曰」** 作为小节标题。

- 先叙后议：先概括篇中要旨，再发议论。
- 议论须有史识——不是感慨，是洞察。
- 可引用国际案例作为对照。
- 篇幅以 100-300 字为宜。
- **纯白话，不仿文言**。

---

## 四、志、表

- **志**记专题。跨越年份的主题（算力变迁、数据之争、开源运动、评价基准演化、社区文化），不宜拆散入编年，单独成志。
- **表**备速查。大事年表、版本沿革表、公司兴替表，以表格形式梳理，不作叙事。

二者均为白话，叙事简洁。

---

## 五、sources/ 归档规范

### 5.1 目录组织

```
sources/
├── 2022/
│   ├── 08/
│   │   ├── index.json            # 该月来源索引
│   │   ├── stability-blog.html   # 网页快照
│   │   └── wiki-sd.html
│   └── 12/
│       └── ...
├── 2023/
│   └── ...
```

- 按 `年份/月份/` 组织，与编年目录一一对应。
- 每个来源文件命名为 `来源域名-文章标识.html`。
- 如引用同一来源的多篇文章，加数字后缀：`verge-01.html`, `verge-02.html`。

### 5.2 快照格式

- **主快照**：HTML 文件（`curl -L` 抓取，保留文本内容即可，不需要 CSS/JS/图片）。
- **辅助快照**：截图（PNG，仅用于 twitter/知乎等动态页面或需要视觉留证的场景）。
- 截图文件较大时（单张 > 1MB），上传到 GitHub Releases，`index.json` 中记录 Release URL。
- 快照文件名不含空格，用 ASCII 字母 + 数字 + 连字符。

### 5.3 index.json 格式

```json
{
  "month": "2022-08",
  "sources": [
    {
      "ref": "[^1]",
      "url": "https://stability.ai/news/stable-diffusion-public-release",
      "title": "Stable Diffusion Public Release",
      "snapshot": "stability-blog.html",
      "screenshot": null,
      "archived_at": "2026-05-25",
      "wayback_url": "https://web.archive.org/web/20260525120000/https://stability.ai/..."
    }
  ]
}
```

- `wayback_url`：通过 Internet Archive Save Page Now API 保存后的永久链接（推荐但不强制）。

### 5.4 归档流程

1. 撰写条目时，用 `tools/validate_links.js` 验证所有 URL 可达性。
2. 用 `curl -L <url> > sources/YYYY/MM/slug.html` 保存文本快照。
3. 更新 `index.json`。
4. 提交 Git 时 HTML 文件一并入库。截图文件大于 1MB 的走 GitHub Releases。

---

## 六、审校规则

### 6.1 交叉验证

- 日期、参数、benchmark 数据至少**两个独立来源**。
- 单源信息必标注"存疑"。
- 自媒体（Twitter/知乎/公众号）可作线索，不可作唯一出处。

### 6.2 笔削

- 有据则书，无据则阙。
- 争议事件须列各方说法。
- 商业宣传声称须与其他独立评测对照后再采信。
