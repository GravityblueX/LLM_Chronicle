{
  "scripts": {
    "test": "node --test tools/validate_links.test.js tools/validate_format.test.js",
    "validate": "npm run validate:format && npm run validate:links",
    "validate:format": "node tools/validate_format.js",
    "validate:links": "node tools/validate_links.js",
    "extract-urls": "node tools/extract_urls.js .",
    "snapshot": "npm run extract-urls && node tools/snapshot.js",
    "snapshot:dry-run": "node tools/snapshot.js --dry-run",
    "snapshot:text-only": "node tools/snapshot.js --text-only",
    "test:snapshot": "node --test tools/snapshot.test.js"
  },
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
