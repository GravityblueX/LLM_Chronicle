# 凡例 v2.0（已废弃）

> 最后修订：2026-05-25
> **此文件已合并入 00_体例.md，保留仅供历史参考。**

## 〇、总则

### 0.1 文体

**全书一律用白话文。** 编年、纪传、志、表、史论，全部用现代汉语写作。

不用文言，不用"论曰""太史公曰"等仿古标题。叙事干净、准确、可读；议论有据、有洞察、不空谈。

### 0.2 真实性原则

每一条事实必须**可追溯**。具体要求：

- **链接可访问**：引用的 URL 在编纂时必须是活链。死链须标注"（链接已失效）"并附存档路径。
- **网页快照留档**：每一条引用的网页，必须在 `sources/` 目录下保存快照（详见 §五）。
- **交叉验证**：关键数据（发布日期、参数规模、benchmark 分数、融资额）至少两个独立来源。单源信息标注"（存疑）"。
- **有据则书，无据则阙**。不确定的事不写，写进去的必须能查回去。

### 0.3 模型优先

大模型纪事的核心对象是**模型**。整理优先级：

1. **模型发布**（新模型、重大版本更新、架构创新）——最高优先级
2. **技术突破**（训练方法、推理优化、开源工具）——高优先级
3. **公司事件**（融资、并购、人事变动、政策封禁）——中优先级
4. **社区/文化事件**（病毒传播、亚文化现象、争议）——选入，作为生态注脚

同一时段内，模型事件优先于公司事件优先于社区事件。这不是说后者不重要——而是说这是一部**模型的历史**，不是一部商业史或文化史。

### 0.4 断限

上起 **2017 年 6 月**（Attention Is All You Need 发表），下迄**编纂之时**。

断限之前的重要前史（深度学习早期发展、word2vec、seq2seq 等），入《志·前史》。

---

## 一、编年

### 1.1 编年是本书的主线

以年-月为基本单位，逐条记录大模型领域的重要事件。读完编年部分即可掌握领域全貌。

编年是骨架，纪传是肌肉——编年告诉你"什么时候发生了什么"，纪传告诉你"为什么重要、怎么演变的"。

### 1.2 目录组织

```
编年/
├── 2022/
│   ├── 08.md          # 2022 年 8 月
│   └── 12.md          # 2022 年 12 月
├── 2023/
│   ├── 02.md
│   ├── 03.md
│   └── 05.md
├── ...
```

**规则**：

- 每年一个文件夹，每月一个文件。
- 文件名统一为 `MM.md`（两位数字月份）。同一月份有多个专题时，在同一文件内用 `##` 二级标题分隔，不拆多个文件。
- 该月无事则不建文件。
- 年份文件夹内始终保留 `.gitkeep`，确保空年份目录不被 Git 忽略。

### 1.3 事件条目格式

每条编年事件采用**单行概述格式**：

```
**YYYY-MM-DD** — 事件概述。关键参与者。出处。[^n]
```

示例：

```
**2017-06-12** — Attention Is All You Need 论文发布。Vaswani et al.（Google Brain / Google Research）提出 Transformer 架构。arXiv:1706.03762[^1]
**2022-11-30** — OpenAI 发布 ChatGPT，基于 GPT-3.5。五天内用户破百万。OpenAI Blog, 2022-11-30[^2]
```

**细则**：

- 日期明确到日；仅有月份的写到月（置于该月开头）；仅有年份的写到年（置于该年开头或末尾，视语境而定）。
- 不确定的日期写"约 XXXX 年 X 月"。
- 出处紧跟在描述后，同一行内给出来源简称 + 注脚编号 `[^n]`。
- 多源交叉验证后的事实不额外标注。单源事件标"（存疑）"。
- 自媒体（Twitter/X、知乎、公众号）可作线索，不可作唯一出处。

### 1.4 专题条目（深度写法）

某些事件无法用一行概述讲清楚——需要交代技术背景、生态影响、后继发展（如 Stable Diffusion、Neuro-sama、DeepSeek-R1）。此时以**专题条目**形式展开。

专题条目格式：

```markdown
# YYYY年MM月

## 专题标题

> 一句话提要（引用块，概括本篇主旨）

### 背景/发布
（叙事文本 + 事件条目）

### 技术要点/成长轨迹
（表格或列表）

### 影响/后继
（分析文本）

### 评曰
（史论，见 §四）

---

*本篇由 [编纂者] 编纂。*

---

[^1]: 出处
[^2]: 出处
```

**要点**：

- 专题条目是编年条目中重要事件的**扩展**，本质仍是编年体的一部分。
- 一个专题条目 = 一个文件的全部内容。一月一事，不混排。
- 专题条目末尾须有出处注脚（Markdown footnote 格式）。
- 专题条目末尾须标注编纂者。

### 1.5 出处注脚规范

使用 Markdown footnote 格式：

```markdown
[^1]: 来源名称，《标题》，发布日期。URL
[^2]: 来源名称，《标题》，发布日期。URL — 网页快照: sources/2025/01/snapshot_xxx.png
```

**格式要求**：

- 来源名称在前（论文作者/媒体名/博客名）
- 标题用书名号
- 日期必填
- URL 必填（死链标注"链接已失效"）
- 有网页快照的，追加 `— 网页快照: sources/路径`

---

## 二、纪传

### 2.1 纪传是编年的补充

编年中写不清楚的重要话题——某公司的兴衰、某模型系列的迭代、某核心人物的去留——单独成传，详述始末。

纪传不作重复铺陈。编年中已有的日期和事件，纪传可以引用，但不必复述。

### 2.2 分类

| 类别 | 对象 | 示例 |
|------|------|------|
| **本纪** | 公司兴衰、核心人物 | 《OpenAI 本纪》《梁文锋本纪》 |
| **世家** | 模型系列源流 | 《GPT 世家》《Llama 世家》《DeepSeek 世家》 |
| **列传** | 单个模型、架构、技术的始末 | 《Transformer 列传》《DeepSeek-R1 列传》《Stable Diffusion 列传》 |

### 2.3 叙事规范

- **全白话**。不模仿文言，不用"初，……""遂……""乃……"等文言虚词。正常现代汉语叙事。
- **以编年为纲**。纪传内部按时间线推进，关键日期可标注公元纪年（YYYY-MM-DD）。
- **首次出现写全称**。公司首次出现写全称+简称（如"OpenAI（奥本哀）"），人物首次出现注其身份，模型首次出现注参数规模与发布日期。
- **叙事与议论分离**。正文只叙事，不夹议论。议论统一放在末尾"评曰"段（见 §四）。
- **不以今日之见褒贬当时之事**。写 2023 年初的 GPT-4 时，不要用 2026 年的视角评判它"当时还很弱"——放到当时的语境里写。

### 2.4 结构

```
# [传名]

## 一、[第一阶段标题]

（叙事）

## 二、[第二阶段标题]

（叙事）

……

## 评曰

（白话史论，见 §四）

---

*本篇由 [编纂者] 编纂。*

---

[^1]: 出处
[^2]: 出处
```

---

## 三、志

### 3.1 志记专题

跨越年份、不宜拆散入编年的主题，独立成志。例如：

- 《志·前史》（2017 年之前的深度学习发展）
- 《志·算力》（GPU 变迁、训练成本演变）
- 《志·数据》（训练数据之争、版权诉讼）
- 《志·开源运动》（开源模型的兴起与生态）
- 《志·评价基准》（benchmark 的演化与争议）
- 《志·社区文化》（HuggingFace、Reddit、知乎、B 站的 AI 社区）

### 3.2 写法

- **白话叙事**，可以比编年更自由——按主题组织而非按时间线。
- 志的末尾可以有"评曰"，也可以没有——如果只是客观梳理数据，不需要议论。
- 引用编年和纪传中的内容时，用 `见《编年·2025年1月》` 格式交叉引用。

---

## 四、史论（"评曰"）

### 4.1 替代"太史公曰"

全书统一用 **"评曰"** 作为史论标题。不模仿"太史公曰""臣光曰"等仿古格式。

"评曰" 可以出现在：

- 编年专题条目末尾
- 纪传（本纪/世家/列传）末尾
- 志的末尾（可选）
- 独立论篇（置于 `论/` 目录）

### 4.2 写法要则

**评曰要写三件事，按顺序：**

1. **概括要旨**——这篇讲了什么，核心事件/人物/模型是什么
2. **分析因果**——为什么这件事重要？它在更大的历史脉络里处于什么位置？
3. **给出判断**——你的看法是什么？不是感慨，是经过推理的结论

**禁止：**

- 空泛感慨（"时代的洪流""技术的伟力"）
- 用典堆砌（没有对应典故不如直说）
- 道德审判（"某某公司贪婪""某某人短视"——你写的是史论，不是檄文）
- 未经事实支撑的断言

**篇幅**：200-500 字。比旧"太史公曰"稍长，因为白话表达需要更多字数来精确。

### 4.3 格式

```markdown
### 评曰

（白话议论，200-500字）
```

示例（改写自现有条目中的"论曰"——DeepSeek-R1 篇）：

> ### 评曰
>
> DeepSeek-R1 有两个贡献。大头是生态上的，小头是技术上的。
>
> 技术上，R1-Zero 证明了推理能力不一定要靠 SFT 教学——纯强化学习、纯规则奖励，也能逼出推理行为。训练过程中自发涌现的"顿悟时刻"（模型自己停下来重新思考）尤其值得注意：这不是人类编码的行为，是 RL 环境与模型能力相互作用的产物。此前涌现多是 benchmark 数字跳变，这一次涌现出来的是一种**行为**。
>
> 生态上，MIT 开源是真正改变格局的决定。OpenAI o1 把推理模型做成了奢侈品——思维链不可见、价格高不可攀、模型权重黑箱。R1 在这个前提下对标 o1 并全部开源，等于把推理模型从奢侈品变成了公共品。英伟达一天蒸发 6000 亿美元，市场恐惧的不是 R1 本身，而是它揭示的事实：最先进的 AI 不一定需要最多的 GPU。此后全球推理模型浪潮——Kimi K1.5、Qwen3-Thinker、Claude extended thinking——无一不受 R1 催化。它定义的不只是一个模型，而是一个品类。

---

## 五、来源归档规范（sources/）

### 5.1 为什么需要 sources/

链接会失效。页面会改版。今天的新闻明天可能被删除。

`sources/` 是本书的**证据链**——每一条引用的网页都应有快照存档，确保五年后仍可验证。

### 5.2 目录组织

```
sources/
├── README.md              # 归档说明
├── index.json             # 来源索引（机器可读）
├── 2022/
│   ├── 08/
│   │   ├── stability-ai-sd-release.html      # 原始 HTML
│   │   ├── stability-ai-sd-release.png        # 页面截图
│   │   └── wikipedia-sd.html
│   └── 12/
│       ├── vice-neurosama.html
│       └── wikipedia-neurosama.html
├── 2023/
│   ├── 02/
│   │   ├── microsoft-new-bing.html
│   │   ├── nyt-roose-bing-chat.html
│   │   └── ...
│   └── ...
└── ...
```

**规则**：

- 按 `年份/月份/` 组织，与编年目录一一对应。
- 每个来源保存**两个文件**：
  - `.html` — 原始 HTML（浏览器"另存为完整网页"）
  - `.png` — 页面截图（全页截图，含 URL 栏）
- 文件命名：`来源简称-文章关键词.html`（小写英文，连字符分隔）。例如 `nyt-roose-bing-chat.html`。
- 每个月底的 `index.json` 自动生成（或手动维护），记录该月所有来源的映射关系。

### 5.3 index.json 格式

```json
{
  "year": 2023,
  "month": 2,
  "entries": [
    {
      "ref": "[^1]",
      "source": "Microsoft Blog",
      "title": "Reinventing search with a new AI-powered Microsoft Bing and Edge",
      "url": "https://blogs.microsoft.com/blog/2023/02/07/...",
      "date_archived": "2026-05-25",
      "files": {
        "html": "microsoft-new-bing.html",
        "screenshot": "microsoft-new-bing.png"
      }
    }
  ]
}
```

### 5.4 归档流程

1. 编纂条目时访问原始链接
2. 浏览器"另存为完整网页" → 保存 `.html` 到对应 `sources/年份/月份/`
3. 浏览器全页截图 → 保存 `.png` 到同一目录
4. 在条目注脚中追加 `— 网页快照: sources/路径`
5. 更新该月的 `index.json`

**如果链接已经失效**：标注"（链接已失效）"，尝试从 Wayback Machine 恢复，并在注脚中注明 `— 存档: web.archive.org/...`。

---

## 六、表

### 6.1 表备速查

以 Markdown 表格形式梳理，不作叙事。置于 `表/` 目录。

规划中的表：

- 《大事年表》（编年条目的浓缩索引）
- 《版本沿革表》（主要模型的发布日期、参数规模、许可证）
- 《公司兴替表》（融资、并购、倒闭时间线）
- 《benchmark 纪录表》（各基准的 SOTA 变迁）

### 6.2 格式

纯 Markdown 表格。第一列通常是时间，便于按时间排序。

---

## 七、审校规则

### 7.1 交叉验证

- 日期、参数、benchmark 数据至少**两个独立来源**
- 单源信息必标注"存疑"
- 自媒体（Twitter/X、知乎、公众号）可作线索，不可作唯一出处

### 7.2 笔削

- 有据则书，无据则阙
- 争议事件须列各方说法，不偏袒
- 商业宣传声称（如"全面超越 GPT-4"）须与其他独立评测对照后再采信

### 7.3 版本管理

- 每篇条目末尾标注编纂者和编纂日期
- 重大修改（内容增删超过 30%）须在 commit message 中说明修改原因
- 事实性错误修正可直接 commit，不视为重大修改

---

## 八、附录：从 v1.0 迁移

### 8.1 已变更的规则

| v1.0 | v2.0 |
|------|------|
| 纪传用浅近文言 | 全白话 |
| "太史公曰" 用标准文言 | "评曰" 用白话 |
| 编年条目无专题格式 | 新增专题条目格式（§1.4） |
| sources/ 无归档规范 | 新增完整归档规范（§五） |
| README 说"一年一个文件" | 统一为"年份文件夹 + 月份文件"（§1.2） |
| 未明确模型优先级 | 新增"模型优先"原则（§0.3） |

### 8.2 已有条目需要更新的内容

- 2023-02（Sydney）、2025-01（DeepSeek-R1）、2026-03（蒸馏攻击）中的 `### 论曰` → `### 评曰`，并将文言议论改写为白话。
- 2026-03-lewm.md → 重命名为 `03-lewm.md`（保持在同一月份文件夹内），或合并到 `03.md`。
- 所有条目追加网页快照后，补充 `— 网页快照: sources/路径` 到注脚。

---

> 体例 v2.0 由 ssg 的 AI 史官·赛希 设计，2026-05-25。
